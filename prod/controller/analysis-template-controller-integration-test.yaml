apiVersion: argoproj.io/v1alpha1
kind: AnalysisTemplate
metadata:
  name: jettison-api-e2e-test
  namespace: jettisonproj
spec:
  args:
  - name: service-name
  metrics:
  - name: jettison-api-e2e-test
    provider:
      job:
        spec:
          backoffLimit: 1
          activeDeadlineSeconds: 600
          template:
            spec:
              containers:
              - name: jettison-api-e2e-test
                image: ghcr.io/jettisonproj/jettison-controller-integration-test:bf7f938edf82532252668e2399440425d46f27bb
                # todo need to target the specified service name
                # env:
                # - name: JETTISON_API_TEST_HOST
                #   value: "{{args.service-name}}"
              restartPolicy: Never
