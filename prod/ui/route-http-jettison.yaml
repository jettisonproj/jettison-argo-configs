apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  namespace: jettisonproj
  name: jettison-ui-prod-http-route
spec:
  parentRefs:
  - name: default-gateway
    namespace: default
    sectionName: http-osoriano-gw
  hostnames:
  - jettison.osoriano.com
  rules:
  - filters:
    - type: RequestRedirect
      requestRedirect:
        scheme: https
        statusCode: 301
